import { View, Text } from 'react-native';

export default function ChefPlaceholder() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Chef home (placeholder)</Text>
    </View>
  );
}